<form method="post" action="{{ path('app_voyage_delete', {'id': av_voyage.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce voyage ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_voyage.id) }}">
    <div class="d-flex justify-content-between">
        <button class="btn btn-danger">Supprimer</button>
    </div>
</form>
