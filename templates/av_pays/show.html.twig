{% extends 'base.html.twig' %}

{% block title %}AvPays{% endblock %}

{% block body %}
    <h1>AvPays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_pay.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ av_pay.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_pays_index') }}">back to list</a>

    <a href="{{ path('app_av_pays_edit', {'id': av_pay.id}) }}">edit</a>

    {{ include('av_pays/_delete_form.html.twig') }}
{% endblock %}
