<form method="post" action="{{ path('app_av_pays_delete', {'id': av_pay.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_pay.id) }}">
    <button class="btn">Delete</button>
</form>
