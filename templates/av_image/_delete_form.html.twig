<form method="post" action="{{ path('app_av_image_delete', {'id': av_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_image.id) }}">
    <button class="btn">Delete</button>
</form>
